name: Update public database website
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  catalog:
    uses: hertigservices/Ascension_preservation/.github/workflows/ascension-db-r2.yml@df632ad6869894e5b0bbd562594a9fcf5b3b764c
    with:
      source_ref: df632ad6869894e5b0bbd562594a9fcf5b3b764c
      icon_base: https://ascension-public-data.ascension-archive.workers.dev/images/icons/
    secrets:
      ASCENSION_PUBLICATION_TOKEN: ${{ secrets.ASCENSION_PUBLICATION_TOKEN }}
