# Working data -- all of it is regenerable from submitted caches, and some of it
# contains player data that must never be committed.
_inbox/
extracted/
merged/
merged.prev/
# Bulk cache files are published through verified GitHub Release packs.
/cachedata/*
!/cachedata/contributions/
ledger.json
MANIFEST.md
config.json
quarantine/

__pycache__/
*.pyc
modes.json
decoded/
# import_world.py output (SQL staging files + a mysqldump backup of YOUR database)
import-out/
